stages:
    - build

build:
    stage: build
    image: node:12
    script:
        - node -v
        - npm -v
        - npm ci
        - npm run build
    tags:
        - 2gis
        - docker
    artifacts:
        paths:
            - dist
            - assets
            - index.html
        expire_in: 5 days
